# Server-side CRM/webhook integration. Configure these as hosted secrets, never in client code.
LEAD_WEBHOOK_URL=
LEAD_WEBHOOK_TOKEN=

# Sanity Studio configuration. These identifiers are public and safe to expose
# to the Studio client; do not add Sanity API tokens here.
SANITY_STUDIO_PROJECT_ID=pkp5ah6s
SANITY_STUDIO_DATASET=production
