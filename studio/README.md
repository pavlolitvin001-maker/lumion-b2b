# LUMION B2B Sanity Studio

1. Copy `.env.example` to `.env` in this folder.
2. Run `npm install`.
3. Run `npm run dev`.

The Studio is connected to Sanity project `pkp5ah6s` and the `production` dataset.
No content schemas or website content are managed here yet.
