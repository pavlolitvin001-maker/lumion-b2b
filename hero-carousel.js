(()=>{const root=document.querySelector('.hero-carousel');if(!root)return;const stage=root.querySelector('.hero-stage'),image=root.querySelector('.hero-current-image'),dots=[...root.querySelectorAll('.hero-progress button')],prev=root.querySelector('.hero-arrow-prev'),next=root.querySelector('.hero-arrow-next');const photos=[{src:'/assets/hero-slide-1.jpg',alt:'Будівля, оформлена гірляндами вночі',position:'54% 51%'},{src:'/assets/hero-slide-2.jpg',alt:'Подвір’я з підсвіченими деревами та фасадом',position:'57% 56%'},{src:'/assets/hero-slide-3.jpg',alt:'Будинок із гірляндами на даху вночі',position:'55% 47%'}];let current=0,startX=0,dragging=false,auto,changing=false;const stop=()=>{if(auto){clearInterval(auto);auto=null}};const paintDots=()=>dots.forEach((dot,i)=>dot.classList.toggle('is-active',i===current));const apply=nextIndex=>{const photo=photos[nextIndex];current=nextIndex;stage.style.backgroundImage=`url("${photo.src}")`;stage.style.backgroundPosition=photo.position;image.src=photo.src;image.alt=photo.alt;image.style.objectPosition=photo.position;image.classList.remove('is-changing');paintDots();changing=false};const show=(index,manual=false)=>{if(manual)stop();if(changing)return;const nextIndex=(index+photos.length)%photos.length;if(nextIndex===current)return;changing=true;image.classList.add('is-changing');window.setTimeout(()=>apply(nextIndex),350)};const move=(direction,manual=false)=>show(current+direction,manual);apply(0);prev.addEventListener('click',()=>move(-1,true));next.addEventListener('click',()=>move(1,true));dots.forEach((dot,i)=>dot.addEventListener('click',()=>show(i,true)));stage.addEventListener('pointerdown',event=>{dragging=true;startX=event.clientX;stage.setPointerCapture(event.pointerId)});const release=event=>{if(!dragging)return;const delta=event.clientX-startX;dragging=false;if(Math.abs(delta)>40)move(delta<0?1:-1,true)};stage.addEventListener('pointerup',release);stage.addEventListener('pointercancel',release);auto=setInterval(()=>move(1),3000)})();
